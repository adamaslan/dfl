# Example environment variables for Gemini proxy
# Copy this to .env.local (Next.js) or .env and fill in your values.

# The API key for your Gemini provider (keep this secret)

# GEMINI_API_KEY=your_gemini_api_key_here

# The full Gemini API URL (provider endpoint). This is optional.
# If you don't set GEMINI_API_URL, the proxy will call Google's Generative Language
# REST endpoint (text-bison model) using the API key.
# If you want to use a different provider or model, set GEMINI_API_URL to the provider's endpoint.
# Example for a generic REST endpoint:
# GEMINI_API_URL=https://api.yourgeminiprovider.com/v1/generate
GEMINI_API_URL=

# NOTE: Do NOT commit your real API key to version control. Use .env.local or your hosting provider's secrets.
